{
  "name": "translateyourreview",
  "version": "1.0.0",
  "description": "Web extension that provides translation for reviews on RateYourMusic",
  "license": "MIT",
  "author": "compscitwilight",
  "scripts": {
    "build:js": "esbuild src/**/*.ts --bundle --outdir=dist",
    "build:assets": "cp -r public/* dist/",
    "build": "npm run build:js && npm run build:assets"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
